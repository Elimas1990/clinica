import { FondoProfDirective } from './fondo-prof.directive';

describe('FondoProfDirective', () => {
  it('should create an instance', () => {
    const directive = new FondoProfDirective();
    expect(directive).toBeTruthy();
  });
});
