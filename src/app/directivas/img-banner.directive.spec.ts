import { ImgBannerDirective } from './img-banner.directive';

describe('ImgBannerDirective', () => {
  it('should create an instance', () => {
    const directive = new ImgBannerDirective();
    expect(directive).toBeTruthy();
  });
});
