import { InfoUserDirective } from './info-user.directive';

describe('InfoUserDirective', () => {
  it('should create an instance', () => {
    const directive = new InfoUserDirective();
    expect(directive).toBeTruthy();
  });
});
